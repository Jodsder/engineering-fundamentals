apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipt-lr-eng.fullname" . }}
  labels:
    {{- include "ipt-lr-eng.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipt-lr-eng.selectorLabels" . | nindent 4 }}
